# Estado — 02/07/2026

Branch de trabalho: **`v1.3-redesign`** (criada a partir da `v1.2-projeto`).
Pontos de retorno: tag `checkpoint/2026-07-02-faca-icones` e branch
`backup/2026-07-02-estavel` (visual 100% original, no remoto).

## Commitado hoje (na v1.2-projeto, já no remoto)

1. `146ad49` — **Mimaki + IECHO juntos** (opção "both": as duas marcas ao mesmo
   tempo, fluxo cortar na Mimaki e refilar na IECHO); "Bolinhas" → "IECHO".
2. `8fea3e8` — **build empacota todos os assets** (logo + 44 SVGs; antes o exe
   saía sem ícones) + **faca com modos** (Automático/Retângulo/Contorno
   justo/suave/simplificado/Faca do cliente — Automático: JPG opaco vira
   retângulo, PNG transparente segue o recorte).
3. `13c06f8` (v1.3-redesign) — **fundação do PrintNest Design System** (tokens +
   QSS premium + canvas claro).

Build gerado e testado: `PrintNest_Build\PrintNest.exe` (117 MB; cópia estável
datada `PrintNest_estavel_2026-07-02.exe`).

## NÃO commitado (working tree da v1.3-redesign — decisão do Philipe: sem commit)

- **Refinamento visual** (estilo "Illustrator 2019→2025", estrutura intacta):
  ribbon com QSS correto, setas de spinbox/combo visíveis (bug clássico de QSS),
  cards do inspector com cabeçalhos coloridos por seção, respiro 12/16px,
  biblioteca com linhas 52px/miniatura 44px, réguas/overlay/guias nos tokens,
  sombra da chapa **vetorial** (fix de travamento no zoom), combo **Tipo de
  faca na barra** ao lado do Gerar Faca, "+" da aba maior.
- **Atalhos Corel**: Ctrl+C/V (copiar/colar peças, Ctrl+D em cadeia), Ctrl+W,
  Ctrl+P, T/B/L/R/C/E (alinhar), F2/F3/Shift+F2/Shift+F4 (zoom), H (mão),
  Alt+setas (pan), Alt+Enter (propriedades do objeto).
- **Correções da auditoria QA** (4 bloqueadores): undo do giro real (QA-01),
  exportação nunca silenciosa (QA-02), faca vazia recusada (QA-03), mesmo
  arquivo 2× soma quantidade (QA-04). Testes de regressão para todos.

## Auditoria QA Master (4 agentes paralelos)

Relatório completo: `docs/qa/RELATORIO-QA-2026-07-02.md`. Resumo: motor
excelente (5.000 peças/65ms), robustez de importação ótima; os 4 bloqueadores
achados foram corrigidos na mesma sessão. Pendências abertas priorizadas:
QA-06 (drift de tokens no canvas), QA-07 (duplicar/undo em cadeia degrada
quadrático), QA-08 (closeEvent/QThread), QA-09 (Alt+O ambíguo), QA-10
(acentuação), QA-11 (segfault de teardown da suíte), QA-12 (clipboard por aba).

## Histórico do dia (decisões)

- Rebuild 2.0 completo (header único, rail, biblioteca em cards) foi
  implementado, apresentado e **revertido** — Philipe preferiu manter a
  estrutura clássica ("é o mesmo PrintNest, só que premium"). O blueprint/
  protótipo navegável ficou publicado como artifact (Fases 1–7) para referência.
- Filosofia acordada: **refinar, não reinventar**.

## Pendências para a próxima sessão

1. Decidir commit do working tree (refinamento + atalhos + correções QA).
2. Pendências QA abertas (acima) — começar por QA-06/QA-07.
3. Paletas de cores + desenhar linhas no arquivo de impressão (adiado).
4. Comercialização: licenciamento, assinatura do exe, beta em gráficas
   (ver `docs/produto/PLANO-COMERCIALIZACAO.md`).
5. Ctrl+Shift+D "Propriedades do documento" conflita com Repetir em grade —
   aguardando decisão (sugerido Ctrl+Shift+P).
