# Estado — 03/07/2026

Branch: **`v1.3-redesign`** (no remoto). Versão do app: **v1.3.0**.
Pontos de retorno: tag `checkpoint/2026-07-02-faca-icones` e branch
`backup/2026-07-02-estavel`.

## O que entrou hoje (tudo commitado e no remoto)

### Ajustes de UI
- Cards do inspector em azul sólido (cor do CTA), mais finos; "+" da aba
  ajustado; "Adicionar arquivos" duplicado removido da ribbon.
- Barrinha de exibição flutuante no canvas (arrastável, colapsa no clique do
  olho) + botão "Visualização" na barra de cima; correção do "sumiu" (re-eleva
  no view_changed). "Rotacionar arquivo" saiu da biblioteca (rotação por peça).
- Barra de abas do inspector com fundo próprio + linha de base.
- Radio marcado redondo (imagem) + lista de chapas do Centro de Exportação
  sempre interativa (não travava com seleção).

### Ícone / versão
- `printnest.ico` gerado do LOGO real (S azul + play) — icone do exe e da janela.
- v1.3.0 no título/About; `build.bat` carimba data/hora no VERSAO.txt.

### Licenciamento (Fase Comercial) — `app/licensing/`
- Modelo: **compra completa + chave de ativação node-locked (SEM trial) +
  garantia de 7 dias** (comercial). Sem licença válida, o EXECUTÁVEL não abre;
  rodando do fonte (dev/testes) não trava.
- Ed25519 offline: app tem só a chave PÚBLICA (`signing.PUBLIC_KEY_HEX`); a
  PRIVADA fica em `tools/license_private_key.pem` (**gitignored, segredo do
  dono**). Fazer backup dela!
- Emissão: `tools/gen_license.py` (CLI), `tools/license_studio.py` (GUI, gera em
  segundos), `tools/issuer.py` (lógica compartilhada, base p/ futuro backend do
  site). `tools/gen_keypair.py` cria novo par (invalida licenças).
- UI: `ActivationDialog` (ID da máquina + colar chave); gate no startup
  (só no exe, via `sys.frozen`); menu Ajuda → Licença.
- Guia: `docs/produto/LICENCIAMENTO.md`. Dep nova: `cryptography` (no .spec).
- 10 testes de licença (par efêmero; forjar/outro-PC rejeitados).

### Faca — múltiplos desenhos por imagem
- Uma folha com N adesivos separados gera **N facas** (peça segue UMA só;
  cortes internos). `detect_contours()` acha todos (filtro de área);
  `Artwork.extra_cuts` + `ImageArtwork.raw_contours` (aditivos, nada quebrou).
  Preview desenha todas; DXF/faca PDF exportam todas.
- **PENDENTE de validação na imagem real do Philipe** (6 adesivos). Se
  fragmentar/juntar (fundo branco vs. transparência), afinar a máscara
  (fechamento morfológico).

### Site de vendas
- `site/` é feito por OUTRO Claude Code (terminal). **Regra combinada:** só ESTA
  sessão commita (Philipe pediu). Copy de vendas: `docs/produto/COPY-SITE-VENDAS.md`
  (preço R$ 397, garantia 7 dias, marcas de registro, export PNG/JPEG/PDF).
  Placeholders a preencher: [SUPORTE] e [LINK_PAGAMENTO].

## Últimos commits
28f1e83 barrinha não some · dbfdaeb multi-faca · 42b7325 emissor+studio ·
fe9e54b licença Ed25519 · 2734f08 versão 1.3.0 · (+ ícone, cards, export).

## Pendências para a próxima sessão
1. **Validar a multi-faca** na imagem real dos 6 adesivos (ajustar máscara se
   preciso).
2. **Refazer o build** com tudo desta sessão (licença + ícone + multi-faca) e
   testar no 2º PC.
3. Site: preencher [SUPORTE] e [LINK_PAGAMENTO]; alinhar quem mexe em `docs/`.
4. Automação da emissão de licença: escolher semi-auto (chave offline) vs.
   full-auto (backend assina) — `tools/issuer.py` é a peça central.
5. Assinatura digital do exe (tira alerta do Defender) + gateway de pagamento.
6. Merge v1.3-redesign → main quando estabilizar.
