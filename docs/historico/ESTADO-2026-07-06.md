# Estado — 06/07/2026

Branch: **`v1.3-redesign`** (no remoto). Versão do app: **v1.3.0**.
Cobre as sessões de 04 a 06/07 (o ESTADO anterior é o de 03/07).
Último commit do dia: `1430213` (curvas Bézier + fusão + fixes do beta).

## ⭐ TARDE DE 06/07 — beta começou + curvas de verdade (LER PRIMEIRO)

### Beta iniciado com o chefe (Danilo — NGD)
- Danilo ativou sozinho via robô (`ngd@nucleografico.com.br`, voucher
  `PNC-4ESK-X6NW`, PC `PN-SBBQ-XF3C-9RWV-344T`). Ele até errou o código na 1ª
  tentativa (mandou o `PNC-XXXX-XXXX` de exemplo) e o robô orientou — fluxo de
  erro validado em situação real. Build no servidor da loja:
  `\\Servidor25\servidor 2025\programas\PrintNest_Version2.0`.
- **Ele está testando e ANOTANDO alterações** — lista de feedback vem aí.
  Triagem combinada: 🔴 defeito / 🟠 melhoria rápida / 🟡 pós-lançamento.
- 3 feedbacks dele JÁ resolvidos (commit 1430213): **mãozinha** ao
  pairar/mover peça (estilo Corel); **"Suavizar" cortava o número** em escala
  125/150% (era setFixedWidth(52) → largura mínima); **canvas "perdido" ao
  restaurar a janela** re-enquadra sozinho (changeEvent + _rescue_lost_view).
- Voucher livre para mais um PC de teste: `PNC-X8D2-VTBM`.

### Curvas Bézier de VERDADE (reclamação "corte meio reto")
- `app/domain/cut/curves.py`: `cubic_segments()` converte a faca poligonal em
  Béziers com tangentes contínuas — passa EXATO pelos nós (contagem de nós
  igual), canto vivo (virada > 32°) preservado, retas exatas (retângulo
  continua retângulo). `has_curves()` decide se vale curvar.
- Saídas: canvas = QPainterPath cúbico; Faca PDF = `draw_bezier` nativo;
  **DXF = SPLINE** (`ezdxf render_splines_and_polylines`); retas seguem
  LWPOLYLINE. ⚠️ **Validar no software de corte da máquina do Danilo se o
  SPLINE importa bem** — senão trocar por arcos (biarc).
- Detecção **sub-pixel** no importador: imagem pequena amplia a máscara com
  interpolação (`MIN_DETECT_SIDE=900`, `UPSCALE_MAX=6`); `SIMPLIFY_MM`
  0.3→0.12; `MAX_DETECT_SIDE` 1000→1600. Facetamento medido num círculo de
  30mm: **0,44 → 0,11mm**.

### Fusão automática de cortes rentes (pedido do Philipe)
- `merge_touching_rect_cuts()` em `app/domain/cut/shared.py`: facas
  retangulares COLADAS (espaçamento 0, tol 0,15mm) viram grade de linhas
  contínuas fora a fora (1 passada por linha); com espaçamento, corte
  individual; contornos curvos/isolados intactos; canto encostado não funde.
- Aplicado no preview (canvas), no DXF e na Faca PDF via `_dxf_payload` —
  os três idênticos. SEM botão: 100% automático.

### Robô/e-mail (manhã, já commitado antes)
- Resposta do robô em **HTML com a logo** (caixa de destaque para a chave).
- Correções: não depende mais da flag "lida" (ProcessedLog por Message-ID) e
  só marca tratado APÓS sucesso (erro = retry).
- **Backup da chave feito**: zip na área de trabalho do Philipe (ele ia copiar
  para pen drive/nuvem — CONFIRMAR amanhã).

### Jurídico
- Rascunhos em `docs/produto/juridico/` (EULA, Privacidade LGPD, Termos de
  Venda/garantia 7 dias). Lacunas dependem do Philipe: canal de suporte,
  CNPJ/MEI (contador) e gateway de pagamento. Depois: advogado valida → site.

## Retomada de amanhã (ordem sugerida)
1. **Build novo** para o servidor do beta (curvas + fusão + fixes — o Danilo
   está testando o build 09:36, de ANTES disso tudo).
2. Receber a **lista de alterações do Danilo** e triar.
3. Confirmar: backup em pen drive/nuvem; SPLINE lê no software de corte.
4. Decisões pendentes do comercial: suporte / CNPJ / gateway (destravam
   jurídico e site).

## 🏆 Marco do dia: fluxo de venda validado de ponta a ponta

Teste de ativação num **2º PC real** (item 1 do checklist comercial) — SUCESSO:

1. `PrintNest_2026-07-03_v1.3.exe` no 2º PC → tela de Ativação bloqueou (gate ok).
2. Cliente (Philipe) enviou o ID da Máquina: `PN-UE66-UB3U-BW73-7FD6`.
3. Emissão: `python tools/gen_license.py --machine-id "PN-..." --customer "..."`
   → chave `PNEST1.<payload>.<assinatura>` (perpétua).
4. Colou em Ativar → liberado; **persistiu após reiniciar o app**.

Ou seja: gate + fingerprint + assinatura + persistência funcionam em máquina
limpa. O emissor com interface é o `tools/license_studio.py` (License Studio):
colar ID → Gerar → Copiar (~30 s por venda).

## 🤖 Segundo marco do dia: ATIVAÇÃO AUTOMÁTICA (robô) validada E2E

Decisão do Philipe: automatizar **no PC dele** (nuvem depois, após conversar
com o chefe — a chave privada não sai da máquina). Implementado e testado com
e-mail real no mesmo dia:

- **Robô** `tools/license_robot.py` (start: `tools/iniciar_robo_licencas.bat`):
  lê a caixa de `ativacao.printnest@gmail.com` (IMAP), valida o **código de
  compra** (`PNC-XXXX-XXXX`, uso único, preso ao 1º PC; mesmo PC = reenvia a
  mesma chave), assina via `issuer.py` e responde com a chave (SMTP).
  Controle próprio de mensagens tratadas (ProcessedLog por Message-ID — NÃO usa
  a flag "lida"; só marca após sucesso, erro = retry). Registros em
  `tools/licenses_emitidas/` (vouchers.json, robo-log.jsonl, processados.json).
- **Códigos de compra**: `tools/vouchers.py` + `tools/gen_vouchers.py -n 20`.
- **App**: botões "Pedir minha chave por e-mail" (mailto pronto com o ID) e
  "Copiar pedido" no ActivationDialog; constante `ACTIVATION_EMAIL` em
  `app/licensing/__init__.py` (mudou o endereço → refazer build).
- **Brecha fechada**: Desativar no exe agora FECHA o app na hora (a checagem
  era só no startup; a sessão aberta continuava usável — Philipe pegou isso).
- Config: `tools/robot_config.json` (senha de app do Gmail; **gitignored**).
- Teste real 06/07: e-mail com ID do 2º PC + `PNC-9FWW-379X` → robô respondeu
  com a chave → ativado no 2º PC. Voucher marcado como usado.
- **Guia completo: `docs/produto/ROBO-ATIVACAO.md`** (rotina, setup do zero,
  problemas comuns, backup). 11 testes novos (21 de licenciamento no total).
- Build atualizado: `PrintNest_Build\PrintNest.exe` 06/07 09:36.

## O que entrou nas sessões 04–06/07 (tudo commitado e no remoto)

### Ferramenta Pontos (F10) — edição de nós da faca, estilo Corel
- Menu Ferramentas + ribbon (ícone `nodes`), atalho **F10**. Escopo **por
  arquivo**: ajusta a faca uma vez, duplica, e as cópias herdam.
- Arrastar nó move (1:1 em qualquer zoom), **duplo-clique no segmento adiciona**
  nó, **duplo-clique no nó remove**. "Voltar ao automático" no card da peça.
- Faca manual (`_faca_manual`) vence o recálculo automático, **sobrevive a
  rotação e redimensionamento** (delta de rotação + escala) e entra no snapshot
  de undo (4-tupla). 3 testes de apresentação.
- **Pendência aceita:** seleção múltipla de nós com marquee ("depois a gente faz").
- **Gap conhecido:** `.printnest` ainda NÃO salva a faca manual no projeto.

### Redução de nós da faca ("muitos nós é ruim pra máquina de corte")
- Pós-processamento Douglas-Peucker em `_finish_contour` remove nós redundantes;
  retas ficam MAIS retas (colineares saem), cantos preservados.
- Seletor **"Nós da faca": Fino / Médio (padrão) / Leve** no card Acabamento
  (`FACA_NODE_TOLERANCES` = 0.1/0.3/0.6 mm), por sessão.
- **Correção importante:** com suavizado > 0 a tolerância normal "des-suavizava"
  a curva (facetada — print do Philipe). Tabela separada
  `FACA_NODE_TOLERANCES_SMOOTH` (0.05/0.08/0.12) preserva a suavização.
  Medido: 256 → 32 nós mantendo a curva lisa.

### Multi-desenho — acabamento
- Marcas de registro **enquadram a peça inteira** (todas as facas, não só a
  maior) — `_faca_rects_of` usa a extensão de todas.
- Elementos pequenos reais (a "estrelinha") agora entram: filtro por **área
  absoluta** `MIN_DESIGN_MM2 = 9.0` (não % da imagem), teto `MAX_DESIGNS = 400`.
  Validado na imagem real dos 6 adesivos.

### Site de vendas
- `site/` agora é um app **React/Vite** (outra sessão desenvolve; ESTA sessão
  commita — regra do Philipe). `node_modules/` no .gitignore.
- Copy oficial: `docs/produto/COPY-SITE-VENDAS.md` (R$ 397). Placeholders
  pendentes: `[SUPORTE]` e `[LINK_PAGAMENTO]`.

## Checklist comercial (status em 06/07)

| # | Item | Status |
|---|---|---|
| 1 | 🔴 Testar ativação no 2º PC | ✅ **FEITO 06/07** |
| 2 | 🔴 Site no ar (placeholders preenchidos) | pendente (outra sessão) |
| 3 | 🔴 Gateway de pagamento (conta + link) | pendente (decisão do Philipe) |
| 4 | 🔴 Fluxo de entrega (manual via WhatsApp/e-mail) | OK para o início |
| 5 | 🔴 **Backup da chave privada** (`tools/license_private_key.pem`) | ⚠️ PENDENTE — lembrar SEMPRE |
| 6 | 🟠 Code signing do exe | pendente |
| 7 | 🟠 EULA + Privacidade (LGPD) + termos da garantia | pendente (posso redigir) |
| 8 | 🟠 Canal de suporte definido | pendente |
| 9 | 🟠 Beta em 2–3 gráficas | pendente |
| — | 🟡 Automação da emissão | ✅ **FEITA 06/07** (robô local; nuvem = conversa com o chefe) |
| — | 🟡 Auto-update / merge main | depois |

## Últimos commits
de33a5a site React/Vite · 1e38d78 fix não des-suaviza · 668214e seletor nós ·
75b3efb pós-simplificação · fd78901 Pontos F10 · 35f0e51 elementos pequenos ·
e463776 marcas multi-desenho.

## Pendências técnicas (backlog)
1. Persistir `_faca_manual` no arquivo `.printnest` (hoje só vive na sessão).
2. Marquee de seleção múltipla na ferramenta Pontos.
3. Nível 3 da redução de nós: ajuste de arcos/Bézier (curvas de verdade,
   ~10–20× menos nós) — ofertado, aguardando pedido.
4. Automação da licença: semi-auto (formulário coleta ID, chave offline) vs.
   full-auto (backend assina — risco: privada no servidor). `tools/issuer.py`
   é a base pronta para qualquer um dos dois.
5. QA-07 definitivo (redesenho incremental da cena) — backlog arquitetural.
6. Merge `v1.3-redesign` → `main` + tag de release quando estabilizar.
