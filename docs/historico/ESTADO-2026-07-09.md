# Estado — 09/07/2026 (fechado e commitado em 13/07 com aprovação)

Branch: **`v1.3-redesign`**. App: **v3.0.0**. Build no ar p/ testes: 13/07
10:25. Regra vigente: NUNCA commitar sem aprovação explícita do Philipe.

## Extras de 13/07 (mesmo pacote, aprovados juntos)
- **Plugin CorelDRAW instalável**: o app "se anuncia" em
  `%APPDATA%\PrintNest\printnest_path.txt` (cliente não configura caminho);
  `corel/instalar_plugin_corel.bat` acha todas as versões do Corel; build gera
  `PrintNest_Build\Plugin CorelDRAW\`. ⚠️ Falta o Philipe gerar `PrintNest.gms`
  UMA vez no Corel dele (instruções no topo de corel/README.md).
- **Navegação livre no canvas** (estilo Corel): sceneRect com folga generosa
  (2× conteúdo + 1000mm) — zoom ancora no mouse em qualquer lugar, pan livre.
- **Ctrl+Z por gesto**: merge de recálculos só dentro de 1,5s (mesmo gesto);
  antes TODOS os ajustes fundiam num comando e o undo "voltava pro início".
- Beta: build 10:25 copiado para outro PC (ativação `PNC-4KPN-Q5UX`).
- PRÓXIMO: tutorial do cliente + tour guiado dentro do app (onboarding).

## O que está pronto na árvore de trabalho (não commitado)

### 1. Theme Engine (brief grande do Philipe, fase 1)
- `app/presentation/themes/` (palettes/manager/settings_dialog): temas
  Claro/Escuro/Midnight/Carbon/Auto, 7 acentos + cor livre, 10 presets,
  16 cores editáveis, exportar/importar JSON, live preview, QSettings.
- `theme.py`: set_palette()/is_dark()/build_qpalette() — QPalette nativa
  derivada dos tokens aplicada junto do QSS (cura miolos brancos no escuro:
  tabela, checkbox, tooltips); grupo Disabled explícito (TEXT_MUTED).
- Logo dupla: `assets/printnest_dark.png` (gerada por assets/make_logo_dark.py,
  miolos vazados) trocada ao vivo em _refresh_logo.
- Menu Opções→Aparência→Tema + Personalizar Interface...
- Inline styles migrados p/ QSS dinâmico: #propBar, #floatBar,
  #measureOverlay, tabPlus, roles accentTag/dot/cardTitle.
- FASE 2 (não feita): escala 90–160%, densidade, cantos, ícones recolorindo
  ao vivo, Quick Search, dockables, microanimações.

### 2. Barra Faca com ESCOPO (estilo Corel) + correções da varredura
- Com peça selecionada: Tipo/Offset/Suavizar/Raio/Canto editam SÓ o arquivo
  (override); sem seleção: documento. Etiqueta mostra o escopo
  ("✂ Faca · documento / este arquivo / manual (Pontos)").
- Suavizar saiu do popup Ajustes → SEMPRE visível ao lado do Offset (pedido).
- Popup "Ajustes ▾": cantos, raio, nós, modo do corte, ajustar chapa.

### 3. VARREDURA anti-regressão (3 agentes; 11 achados reais, 9 corrigidos)
- 🔴 _selected_path nunca limpava (barra editava "arquivo fantasma", até de
  outra aba) → limpo na desseleção e na troca de aba.
- 🔴 Override era CÓPIA COMPLETA → congelava recorte/giro/canto globais →
  virou override ESPARSO ({**global, **override} em _params_for).
- 🔴 Abrir projeto marcava _dirty → modal fantasma "salvar alterações?" ao
  fechar → _dirty=False movido p/ DEPOIS de _populate_files.
- 🟠 Canto da barra ignora escopo → corrigido; remover último arquivo deixava
  barra visível/hint velho → _update_property_bar no ramo; disabled sem cinza
  (QPalette Disabled); ícones congelados na cor clara (default resolvido em
  runtime + icons.clear_cache() na troca); leak de conexão theme_changed
  (disconnect no closeEvent); aviso "faca manual" na etiqueta; tooltip do
  "Usar padrão do documento" agora avisa que apaga TUDO do arquivo.

### 4. Persistência do .printnest (perda de dados — corrigida)
- ProjectDocument ganhou file_overrides + faca_manual (aditivos, projetos
  antigos abrem); _collect/_apply_project serializam/restauram (contours de
  Pontos em listas [[x,y]]). Teste de roundtrip novo.

## Retomada
0. LIGAR O ROBÔ (não volta sozinho): `.venv\Scripts\python.exe -u
   tools\license_robot.py` em background; log em tools/licenses_emitidas/.
1. Philipe testa o app aberto → "aprovado" → suíte completa → COMMIT do
   pacotão + push → build v3.0 novo → servidor do beta.
2. Comercial: 3 decisões do Philipe (suporte, CNPJ/MEI, gateway — sugerido
   Kiwify/Hotmart); backup da chave privada no pen drive (CONFIRMAR!);
   cobrar lista de alterações do Danilo (beta).
3. Depois: QA Fase 2 (🟠), Theme Fase 2, isolamento QSettings nos testes.
