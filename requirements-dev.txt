-r requirements.txt
pytest>=8.0
pytest-cov>=5.0
ruff>=0.5
# juiz independente dos testes de PDF + gerador do Tutor IA (so build/dev;
# NUNCA entra no executavel distribuido — licenca AGPL)
PyMuPDF>=1.24
