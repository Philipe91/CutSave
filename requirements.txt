# Stack de runtime aprovado. Instalado no ambiente; cada lib passa a ser
# usada na fase em que entra (ver docs/ROADMAP.md). SQLite e stdlib (sqlite3).
PySide6>=6.6
shapely>=2.0
# PDF: leitura/render pelo motor do Chrome (Apache/BSD) e escrita pikepdf
# (MPL-2.0) — livres para uso comercial; PyMuPDF (AGPL) saiu do runtime.
pypdfium2>=5.0
pikepdf>=9.0
# headless evita conflito de plugins Qt com o PySide6 (mesma API de cv2)
opencv-python-headless>=4.9
Pillow>=10.0
numpy>=1.26
ezdxf>=1.1
# nesting true-shape (Modo Corte): offset + Minkowski/NFP + booleanas (Clipper)
pyclipper>=1.4
# importacao de SVG (Fase 3): aplica transforms e resolve unidades (MIT)
svgelements>=1.9
# texto digitado -> curvas (Fase 3C): leitura de glifos TTF/OTF (MIT)
fonttools>=4.63
# licenciamento: assinatura Ed25519 das licencas (app carrega so a chave publica)
cryptography>=42.0
